#include <stdio.h>
#include <stdlib.h>

int main(){
	
	int n;
	int m;

	printf("Introduzca el números de filas: ");
	scanf("%d", &n);

	prinft("Introduzca el número de columnas: ");
	scanf("%d", &m);

	int ** arr = (int **) malloc (n * sizeof(int))

}